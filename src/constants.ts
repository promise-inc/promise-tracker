export const DEFAULT_API_URL = 'http://localhost:3001/api';
export const HEARTBEAT_INTERVAL = 120; // seconds (normal activity)
export const WRITE_INTERVAL = 30; // seconds (write activity)
export const GAP_THRESHOLD = 5 * 60; // 5 minutes in seconds
export const TOKEN_KEY = 'promise-tracker.token';
